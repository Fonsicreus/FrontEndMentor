{
  "name": "front-end-mentor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview"
  },
  "dependencies": {
    "vite": "^8.0.1"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
